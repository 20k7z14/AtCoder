import sys
import os
f = open('test.txt','r')
sys.stdin = f

S = input()
cnt = 0
print(S)
for i in S:
    if
        S.pop(i+1)
        S.pop(i)
        print(S)
        cnt += 2
    else:
        continue
print(cnt)
